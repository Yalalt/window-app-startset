import { RootLayout, SideBar } from './components'

function App() {
  return (
    <RootLayout>
      <SideBar className="p-2 border-4 border-red-500">Sidebar</SideBar>
    </RootLayout>
  )
}

export default App
